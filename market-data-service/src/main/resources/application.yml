server:
  port: 8083

spring:
  application:
    name: market-data-service

  datasource:
    url: jdbc:postgresql://localhost:5432/investtrack_marketdata
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

  data:
    redis:
      host: localhost
      port: 6379
      timeout: 5000ms

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

# Kite Connect API Configuration
kite:
  api-key: ${KITE_API_KEY:your_kite_api_key}
  api-secret: ${KITE_API_SECRET:your_kite_api_secret}
  redirect-url: http://localhost:8083/kite/callback
  login-url: https://kite.zerodha.com/connect/login

# Price Cache Configuration
price-cache:
  ttl-seconds: 60
  reconciliation-interval-ms: 300000

# Actuator
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: when-authorized

# Logging
logging:
  level:
    com.investtrack.marketdata: DEBUG
    org.springframework.data.redis: INFO
